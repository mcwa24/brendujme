import { Inter } from "next/font/google";

/** Isti font kao bilbord.rs Ghost tema */
export const inter = Inter({
  subsets: ["latin", "latin-ext"],
  display: "swap",
  variable: "--font-inter",
});
